[project]
name = "obsidian-tools"
version = "0.1.0"
description = "Tools to manage my Obsidian vault"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]
